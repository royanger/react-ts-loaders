import * as React from 'react';
import './loaders.css';

const Loader = (props) => {
  return (
    <div className='loader'>
      This is a test of the actual component inside lib.
    </div>
  );
};

export default Loader;
